[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "posttrain-qualification-fixtures"
version = "0.0.1"
description = "Unpublished synthetic fixtures for Posttrain qualification only"
requires-python = ">=3.12"

[tool.setuptools]
py-modules = ["deterministic_turn_fixture"]
