# Training base configs

Reusable SFT, DPO, and GRPO defaults owned by `packages/train` belong here. Model-specific overlays may extend one parent config. Jobs provide datasets, environment references, exact overrides, and stage order.
