# Evaluation base configs

Reusable general-evaluation selections owned by `packages/eval` belong here. They reference independently published Verifiers environment packages. Domain-specific selections and thresholds remain with the fine-tuning job.
