# /// script
# requires-python = ">=3.11"
# dependencies = ["openai", "mcp", "httpx", "tenacity"]
# ///

# Dependency-only build input. At runtime Verifiers writes its pinned null
# harness at the matching content-addressed path before checking this environment.
