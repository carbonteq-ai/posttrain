project_id: posttrain-lab
work_package_id: screen/gemma4-12b-it/generation-smoke-qualification
stage: screen
description: Launch the pinned Gemma 4 Unified 12B checkpoint and complete one bounded text-only generation.
recipe:
  type: inline
  id: recipes/gemma4-12b-it-generation-smoke-qualification@1
  revision: "1"
  stage: screen
  seats:
    inference: inference
  jobs:
    - id: smoke
      kind: serve.smoke
      definition: serve/vllm-generation-smoke@1
  expected_artifacts:
    - terminal generation response
bindings:
  inference:
    type: ref
    family: inference
    id: inference/gemma4-12b-it-vllm-screen@1
enabled_optional_jobs: []
metadata:
  question: Can the current serving capsule load Gemma 4 Unified and produce a nonempty text response through the model's tokenizer contract?
  labels:
    - qualification
    - gemma4
    - serve-smoke
