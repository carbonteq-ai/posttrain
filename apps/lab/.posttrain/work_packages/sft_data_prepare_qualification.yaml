project_id: posttrain-lab
work_package_id: screen/posttrain-sft-smoke/data-prepare-qualification
stage: screen
description: Validate and retain the packaged supervised fixture as a canonical digest-addressed dataset snapshot.
recipe:
  type: inline
  id: recipes/posttrain-sft-smoke-data-prepare@1
  revision: "1"
  stage: screen
  seats:
    dataset: dataset
    target: target
  jobs:
    - id: prepare
      kind: data.prepare
      definition: data/canonicalize-supervised@1
  expected_artifacts:
    - canonical dataset snapshot
bindings:
  dataset:
    type: ref
    family: dataset
    id: datasets/posttrain-sft-smoke@1
  target:
    type: ref
    family: target
    id: targets/local-cuda-8gb
enabled_optional_jobs: []
metadata:
  question: Can the data capsule validate its packaged bytes and retain one reproducible canonical dataset artifact?
  labels:
    - qualification
    - current-capsule
    - data-prepare
