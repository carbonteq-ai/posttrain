project_id: posttrain-lab
work_package_id: eval/qwen3.5-2b/gsm8k-managed-qualification
stage: qualify
description: Launch Qwen 3.5 2B through the selected vLLM binding and retain a bounded native GSM8K Verifiers evaluation.
recipe:
  type: inline
  id: recipes/qwen3.5-2b-gsm8k-managed-eval@1
  revision: "1"
  stage: qualify
  seats:
    model: model
    evaluation_inference: inference
    target: target
    evaluation_plan: evaluation
    environment: environment
  jobs:
    - id: evaluate
      kind: eval.domain
      definition: eval/verifiers-managed@1
  expected_artifacts:
    - native evaluation result
bindings:
  model:
    type: ref
    family: model
    id: models/qwen3.5-2b@bf16
  evaluation_inference:
    type: ref
    family: inference
    id: inference/qwen3.5-2b-vllm-eval@2
  target:
    type: ref
    family: target
    id: targets/local-cuda-8gb
  evaluation_plan:
    type: ref
    family: evaluation
    id: gsm8k-eval-qualification-v1
  environment:
    type: ref
    family: environment
    id: gsm8k-eval-qualification
enabled_optional_jobs: []
metadata:
  question: Can the packed managed-evaluation capsule launch its own endpoint and persist bounded native Verifiers evidence?
  labels:
    - qualification
    - evaluation
    - gsm8k
    - managed-endpoint
