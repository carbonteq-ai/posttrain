project_id: posttrain-lab
work_package_id: eval/lfm2.5-2.6b/automationbench-context-canary-64k-v1
stage: qualify
description: Two-task, one-repetition gate for 64K LFM evaluation serving with 20-turn episodes, on the tasks whose step-20 VORTEX v4 episodes reached the longest prompts at 12 turns.
recipe:
  type: inline
  id: recipes/lfm2.5-2.6b-automationbench-context-canary-64k-v1@1
  revision: "1"
  stage: qualify
  seats:
    model: model
    evaluation_inference: inference
    target: target
    evaluation_plan: evaluation
    environment: environment
  jobs:
    - id: evaluate
      kind: eval.general
      definition: eval/verifiers-managed-general@1
  expected_artifacts: [native evaluation result]
bindings:
  model: {type: ref, family: model, id: models/lfm2.5-2.6b@bf16}
  evaluation_inference: {type: ref, family: inference, id: inference/lfm2.5-2.6b-vllm-automationbench-eval-local@4}
  target: {type: ref, family: target, id: targets/carbonteq-rtx-pro-6000-96gb}
  evaluation_plan: {type: ref, family: evaluation, id: lfm26-automationbench-context-canary-64k-v1}
  environment: {type: ref, family: environment, id: automationbench-lfm26-context-canary-64k-v1}
enabled_optional_jobs: []
metadata:
  evaluation_population: context-canary-2
  policy_call_output_budget: "8192"
  episode_output_budget: "32768"
  max_turns: "20"
  labels: [qualification, evaluation, automationbench, lfm2.5, vortex, context64k, canary]
