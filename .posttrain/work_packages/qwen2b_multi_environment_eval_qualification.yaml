project_id: foundation-models
work_package_id: eval/qwen3.5-2b/multi-environment-qualification
stage: qualify
description: Pack two environments from one pinned Verifiers checkout and execute the bounded Reverse Text evaluation cell.
recipe:
  type: inline
  id: recipes/qwen3.5-2b-multi-environment-eval@1
  revision: "1"
  stage: qualify
  seats:
    model: model
    evaluation_inference: inference
    target: target
    evaluation_plan: evaluation
    environment: environment
  jobs:
    - id: evaluate-reverse-text
      kind: eval.general
      definition: eval/verifiers-managed-general@1
  expected_artifacts:
    - native evaluation result
bindings:
  model:
    type: ref
    family: model
    id: models/qwen3.5-2b@bf16
  evaluation_inference:
    type: ref
    family: inference
    id: inference/qwen3.5-2b-vllm-eval@1
  target:
    type: ref
    family: target
    id: targets/local-cuda-8gb
  evaluation_plan:
    type: ref
    family: evaluation
    id: two-environment-eval-qualification-v1
  environment:
    type: ref
    family: environment
    id: reverse-text-eval-qualification
enabled_optional_jobs: []
metadata:
  question: Can one actual-job capsule install two environment wheels from one immutable checkout and execute the selected second cell?
  labels:
    - qualification
    - evaluation
    - multi-environment
    - reverse-text
