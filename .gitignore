# Python / tooling
.venv/
.venvs/
__pycache__/
*.py[cod]
*.egg-info/
.python-version
.env
.env.*
!.env.example
# Protected project runtime values.  `posttrain init` writes this rule into a
# generated project's own .gitignore; the framework checkout needs it too
# because apps/lab is a real project that predates init.
posttrain.env
!posttrain.env.example
.mypy_cache/
.ruff_cache/
.pytest_cache/
.import_linter_cache/
.coverage
.coverage.*
htmlcov/
uv.lock.bak

# JavaScript / frontend
node_modules/
dist/
.vite/
playwright-report/
test-results/
blob-report/

# Models / data / run outputs
outputs/
checkpoints/
artifacts/
wandb/
/trackio/
**/.posttrain/state/
*.pt
*.safetensors
*.bin

# Local agent tooling
.agents/
.codex/

# Local-only secrets and host dumps (never commit)
ops/ops-server.txt
ops/remote-server.txt
ops/*.txt

# Editor design canvases (local only)
*.pen

# OS
.DS_Store
